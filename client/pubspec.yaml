# build with Flutter 3.7.12
name: tommypush
description: Simple Push Notifications receiver

version: 23.5.16+6

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fluttertoast: 8.2.1         # toast messages
  firebase_core: 2.13.0       # push notifications
  firebase_messaging: 14.6.1  # push notifications

dev_dependencies:
  flutter_lints: 2.0.0

flutter:
  uses-material-design: true
